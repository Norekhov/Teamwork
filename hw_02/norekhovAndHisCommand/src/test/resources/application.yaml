spring:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    database: POSTGRESQL
    show-sql: true
    hibernate:
            ddl-auto:
              create-drop
  h2:
    console:
      enabled: true
